import { Gate } from '@hubspot/cms-js-internal';
export default function createShadowDevServerRoutes(devServerPort: number, projectName: string, gates: Gate[]): Promise<import("express-serve-static-core").Router>;
