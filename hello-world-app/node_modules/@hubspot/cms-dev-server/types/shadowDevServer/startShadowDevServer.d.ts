import { Gate } from '@hubspot/cms-js-internal';
export default function startShadowDevServer(devServerPort: number, shadowPort: number, projectName: string, jsRenderingGates: Gate[]): Promise<void>;
